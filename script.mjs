import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); supabase.auth.signInWithPassword({ email: 'admin@kafaaat.com', password: 'Password123!' }).then(console.log).catch(console.error);
